dependencies:

alacritty
mako
bemenu
app-launch from https://github.com/u296/app-launch
loginctl
(sway)
swaylock
swaybar

things to know:

on startup, $HOME/.config/guistart/*.sh are run
on exit, $HOME/.config/guistop/*.sh are run
